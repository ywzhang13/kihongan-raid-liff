# KiHongan 遠征報名系統 - 完整部署指南

## 📋 目錄
1. [系統架構](#系統架構)
2. [前置準備](#前置準備)
3. [資料庫設定](#資料庫設定)
4. [後端部署](#後端部署)
5. [前端部署](#前端部署)
6. [LINE LIFF 設定](#line-liff-設定)
7. [測試與驗證](#測試與驗證)
8. [常見問題](#常見問題)

---

## 系統架構

```
LINE 使用者
    ↓
LINE LIFF (前端)
    ↓
Spring Boot API (後端)
    ↓
PostgreSQL 資料庫
```

---

## 前置準備

### 1. 需要的帳號
- ✅ LINE Developers 帳號 (https://developers.line.biz/)
- ✅ 雲端主機帳號（選擇一個）：
  - Render (推薦，免費方案) - https://render.com
  - Fly.io - https://fly.io
  - Railway - https://railway.app
  - Zeabur - https://zeabur.com
- ✅ PostgreSQL 資料庫（選擇一個）：
  - Supabase (推薦，免費方案) - https://supabase.com
  - Neon - https://neon.tech
  - 或使用主機提供的資料庫

### 2. 本地開發工具
- Java 17+
- Maven 3.6+
- Git

---

## 資料庫設定

### 步驟 1: 建立 Supabase 專案

1. 前往 https://supabase.com
2. 點擊「Start your project」
3. 建立新專案，記下：
   - Database Password
   - Project URL
   - Connection String

### 步驟 2: 執行資料庫初始化

1. 在 Supabase Dashboard，進入「SQL Editor」
2. 執行以下 SQL（或使用專案中的 `init-db.sql`）：

```sql
-- Users table
CREATE TABLE users (
    id BIGSERIAL PRIMARY KEY,
    line_user_id VARCHAR(64) UNIQUE NOT NULL,
    name TEXT,
    picture TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Characters table
CREATE TABLE characters (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL REFERENCES users(id),
    name TEXT NOT NULL,
    job TEXT,
    level INTEGER,
    is_default BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Raids table
CREATE TABLE raids (
    id BIGSERIAL PRIMARY KEY,
    title TEXT NOT NULL,
    subtitle TEXT,
    boss TEXT,
    start_time TIMESTAMPTZ NOT NULL,
    created_by BIGINT NOT NULL REFERENCES users(id),
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Raid signups table
CREATE TABLE raid_signups (
    id BIGSERIAL PRIMARY KEY,
    raid_id BIGINT NOT NULL REFERENCES raids(id) ON DELETE CASCADE,
    character_id BIGINT NOT NULL REFERENCES characters(id),
    status TEXT DEFAULT 'confirmed',
    created_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(raid_id, character_id)
);

-- Indexes
CREATE INDEX idx_characters_user_id ON characters(user_id);
CREATE INDEX idx_raids_start_time ON raids(start_time);
CREATE INDEX idx_raid_signups_raid_id ON raid_signups(raid_id);
CREATE INDEX idx_raid_signups_character_id ON raid_signups(character_id);
```

### 步驟 3: 取得連線資訊

在 Supabase Dashboard → Settings → Database，找到：
- Host
- Database name
- Port
- User
- Password

組成連線字串：
```
jdbc:postgresql://[HOST]:[PORT]/postgres?sslmode=require
```

---

## 後端部署

### 方案 A: 使用 Render (推薦)

#### 步驟 1: 準備程式碼

1. 確保你的程式碼已推送到 GitHub
2. 確認 `pom.xml` 中的打包設定正確

#### 步驟 2: 建立 Render 服務

1. 前往 https://render.com
2. 點擊「New +」→「Web Service」
3. 連接你的 GitHub repository
4. 設定：
   - **Name**: `kihongan-raid-system`
   - **Environment**: `Java`
   - **Build Command**: `mvn clean package -DskipTests`
   - **Start Command**: `java -jar target/raid-system-0.0.1-SNAPSHOT.jar`
   - **Instance Type**: Free

#### 步驟 3: 設定環境變數

在 Render Dashboard → Environment，新增：

```bash
DATABASE_URL=jdbc:postgresql://your-supabase-host:5432/postgres?sslmode=require
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=你的資料庫密碼
JWT_SECRET=請用隨機字串至少32字元
LINE_CHANNEL_ID=2009058924
```

生成 JWT_SECRET 的方法：
```bash
# 在終端機執行
openssl rand -base64 32
```

#### 步驟 4: 部署

1. 點擊「Create Web Service」
2. 等待建置完成（約 5-10 分鐘）
3. 記下你的後端 URL，例如：`https://kihongan-raid-system.onrender.com`

#### 步驟 5: 測試後端

```bash
curl https://你的後端URL/raids
```

應該會回傳空陣列 `[]`

---

### 方案 B: 使用 Fly.io

```bash
# 1. 安裝 flyctl
curl -L https://fly.io/install.sh | sh

# 2. 登入
flyctl auth login

# 3. 初始化專案
flyctl launch

# 4. 設定環境變數
flyctl secrets set DATABASE_URL="jdbc:postgresql://..."
flyctl secrets set DATABASE_USERNAME="postgres"
flyctl secrets set DATABASE_PASSWORD="你的密碼"
flyctl secrets set JWT_SECRET="你的JWT密鑰"
flyctl secrets set LINE_CHANNEL_ID="2009058924"

# 5. 部署
flyctl deploy
```

---

## 前端部署

### 方案 A: 使用 GitHub Pages (推薦)

#### 步驟 1: 修改前端設定

編輯 `frontend/app.js`，將 API_BASE_URL 改為你的後端 URL：

```javascript
const API_BASE_URL = 'https://你的後端URL';  // 例如：https://kihongan-raid-system.onrender.com
```

#### 步驟 2: 推送到 GitHub

```bash
git add frontend/
git commit -m "Update API URL for production"
git push
```

#### 步驟 3: 啟用 GitHub Pages

1. 前往 GitHub repository → Settings → Pages
2. Source 選擇「Deploy from a branch」
3. Branch 選擇「main」，資料夾選擇「/frontend」
4. 點擊「Save」
5. 等待部署完成，記下你的前端 URL，例如：
   ```
   https://你的GitHub帳號.github.io/KiHongan/
   ```

---

### 方案 B: 使用 Netlify

1. 前往 https://netlify.com
2. 點擊「Add new site」→「Import an existing project」
3. 連接 GitHub repository
4. 設定：
   - **Base directory**: `frontend`
   - **Build command**: (留空)
   - **Publish directory**: `.`
5. 部署

---

## LINE LIFF 設定

### 步驟 1: 建立 LINE Login Channel

1. 前往 https://developers.line.biz/console/
2. 選擇你的 Provider（或建立新的）
3. 點擊「Create a new channel」
4. 選擇「LINE Login」
5. 填寫資訊：
   - **Channel name**: KiHongan 遠征報名系統
   - **Channel description**: 楓之谷遠征隊報名管理系統
   - **App types**: 勾選「Web app」
6. 建立後，記下：
   - **Channel ID**
   - **Channel Secret**

### 步驟 2: 建立 LIFF App

1. 在 LINE Login Channel 頁面，點擊「LIFF」分頁
2. 點擊「Add」
3. 填寫：
   - **LIFF app name**: KiHongan 遠征報名
   - **Size**: Full
   - **Endpoint URL**: `https://你的前端URL` (GitHub Pages 或 Netlify URL)
   - **Scope**: 勾選 `profile` 和 `openid`
   - **Bot link feature**: Optional (可選)
4. 點擊「Add」
5. 記下 **LIFF ID**（格式：`1234567890-abcdefgh`）

### 步驟 3: 設定 Callback URL

1. 回到 Channel 設定頁面
2. 在「LINE Login」分頁
3. **Callback URL** 設定為：
   ```
   https://你的前端URL
   ```

### 步驟 4: 更新前端 LIFF ID

編輯 `frontend/app.js`，找到 `initializeLiff()` 函數：

```javascript
async function initializeLiff() {
    try {
        // 替換為你的 LIFF ID
        await liff.init({ liffId: '你的LIFF-ID' });
        
        if (!liff.isLoggedIn()) {
            liff.login();
            return;
        }
        
        // 取得使用者資訊
        const profile = await liff.getProfile();
        const idToken = liff.getIDToken();
        
        // 呼叫後端登入 API
        const response = await fetch(`${API_BASE_URL}/auth/line`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                idToken: idToken,
                userId: profile.userId,
                name: profile.displayName,
                picture: profile.pictureUrl
            })
        });
        
        if (!response.ok) {
            throw new Error('登入失敗');
        }
        
        const data = await response.json();
        appToken = data.appToken;
        currentUser = {
            lineUserId: data.lineUserId,
            userDbId: data.userDbId,
            name: profile.displayName,
            picture: profile.pictureUrl
        };
        
        showMessage('userStatus', `✅ 已登入: ${currentUser.name}`, 'success');
        
        // 自動載入初始資料
        loadMyCharacters();
        loadRaids();
        
    } catch (error) {
        console.error('LIFF 初始化失敗:', error);
        showMessage('userStatus', '❌ 初始化失敗: ' + error.message, 'error');
    }
}
```

### 步驟 5: 重新部署前端

```bash
git add frontend/app.js
git commit -m "Configure LIFF for production"
git push
```

---

## 測試與驗證

### 1. 測試後端 API

```bash
# 測試取得遠征列表
curl https://你的後端URL/raids

# 應該回傳 []
```

### 2. 測試前端

1. 用手機開啟 LINE
2. 在任何聊天室傳送你的 LIFF URL：
   ```
   https://liff.line.me/你的LIFF-ID
   ```
3. 點擊連結，應該會：
   - 要求 LINE 登入授權
   - 顯示「✅ 已登入: 你的名字」
   - 可以建立角色
   - 可以建立遠征
   - 可以報名遠征

### 3. 測試完整流程

1. **建立角色**：
   - 輸入角色名稱、職業、等級
   - 點擊「建立角色」
   - 應該在列表中看到新角色

2. **建立遠征**：
   - 選擇 Boss
   - 輸入備註
   - 選擇時間
   - 點擊「建立遠征」
   - 應該在列表中看到新遠征

3. **報名遠征**：
   - 點擊「✅ 我要參加」
   - 選擇角色
   - 應該顯示「✅ 報名成功！」

4. **查看報名名單**：
   - 點擊「👥 查看報名」
   - 應該看到報名的角色資訊

---

## 分享給 LINE 群組成員

### 方法 1: 直接分享 LIFF URL

在 LINE 群組中傳送：
```
🎮 KiHongan 遠征報名系統
https://liff.line.me/你的LIFF-ID

點擊連結即可使用！
```

### 方法 2: 建立 LINE Bot (進階)

如果想要更好的整合體驗，可以建立 LINE Bot：

1. 在 LINE Developers Console 建立 Messaging API Channel
2. 設定 Rich Menu，加入 LIFF 連結
3. 將 Bot 加入群組
4. 成員點擊 Rich Menu 即可開啟系統

---

## 常見問題

### Q1: 前端無法連接後端

**檢查項目**：
1. 後端是否正常運行？
   ```bash
   curl https://你的後端URL/raids
   ```
2. `frontend/app.js` 中的 `API_BASE_URL` 是否正確？
3. 後端 CORS 設定是否允許前端網域？

**解決方法**：
檢查 `src/main/java/com/kihongan/raidsystem/config/WebConfig.java`：
```java
@Override
public void addCorsMappings(CorsRegistry registry) {
    registry.addMapping("/**")
            .allowedOrigins(
                "https://你的前端URL",
                "https://liff.line.me"
            )
            .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
            .allowedHeaders("*")
            .allowCredentials(true);
}
```

### Q2: LINE 登入失敗

**檢查項目**：
1. LIFF ID 是否正確？
2. Callback URL 是否設定正確？
3. 是否在 LINE 應用程式中開啟？

### Q3: 資料庫連線失敗

**檢查項目**：
1. DATABASE_URL 格式是否正確？
2. 是否包含 `?sslmode=require`？
3. 密碼是否正確？
4. Supabase 是否允許外部連線？

### Q4: 排程器沒有執行

**檢查項目**：
1. 確認 `RaidSystemApplication.java` 有 `@EnableScheduling` 註解
2. 檢查伺服器時區設定
3. 查看後端日誌

### Q5: Render 免費方案會休眠

Render 免費方案在 15 分鐘無活動後會休眠，下次訪問需要等待喚醒（約 30 秒）。

**解決方法**：
- 升級到付費方案
- 使用 UptimeRobot 等服務定期 ping 你的後端

---

## 維護與監控

### 查看後端日誌

**Render**:
1. 進入 Dashboard
2. 選擇你的服務
3. 點擊「Logs」分頁

**Fly.io**:
```bash
flyctl logs
```

### 資料庫備份

**Supabase**:
1. 進入 Dashboard → Database → Backups
2. 可以手動建立備份
3. 免費方案保留 7 天備份

### 更新程式碼

```bash
# 1. 修改程式碼
# 2. 提交到 Git
git add .
git commit -m "Update features"
git push

# 3. Render 會自動重新部署
# 4. Fly.io 需要手動部署
flyctl deploy
```

---

## 安全性建議

1. ✅ **JWT_SECRET**: 使用強隨機字串，至少 32 字元
2. ✅ **HTTPS**: 確保前後端都使用 HTTPS
3. ✅ **環境變數**: 不要將敏感資訊提交到 Git
4. ✅ **CORS**: 只允許信任的網域
5. ✅ **定期更新**: 定期更新依賴套件

---

## 成本估算

### 免費方案
- **Render**: 免費（有休眠限制）
- **Supabase**: 免費（500MB 資料庫，無限 API 請求）
- **GitHub Pages**: 免費
- **總成本**: $0/月

### 付費方案（建議用戶增加時升級）
- **Render**: $7/月（無休眠）
- **Supabase**: $25/月（8GB 資料庫）
- **總成本**: $32/月

---

## 支援

如有問題，請查看：
- README.md
- API.md
- GitHub Issues

或聯繫開發者。

---

## 檢查清單

部署前確認：

- [ ] 資料庫已建立並初始化
- [ ] 後端已部署並可訪問
- [ ] 前端已部署並可訪問
- [ ] LIFF App 已建立
- [ ] LIFF ID 已設定到前端
- [ ] API_BASE_URL 已更新
- [ ] CORS 設定正確
- [ ] 環境變數都已設定
- [ ] 測試登入功能正常
- [ ] 測試建立角色功能正常
- [ ] 測試建立遠征功能正常
- [ ] 測試報名功能正常
- [ ] 已分享 LIFF URL 給群組成員

完成以上步驟後，你的系統就可以給 LINE 群組成員使用了！🎉
